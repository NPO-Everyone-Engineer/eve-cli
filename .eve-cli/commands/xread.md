---
description: Read public information from X using xurl
allowed-tools: [Bash]
---
Use `xurl` to fetch read-only information from X for this request:

$ARGUMENTS

Requirements:
- Use only read-only X API endpoints
- Do not post, delete, or modify anything
- If username-based tweet listing is needed, resolve the user ID first
- If `xurl` is not installed or auth is missing, say that clearly
- Summarize the result in Japanese after fetching
