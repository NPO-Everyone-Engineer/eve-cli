---
description: Fix a bug from root cause, not just the symptom
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash]
---
Fix this bug from the root cause:

$ARGUMENTS

Requirements:
- Start by explaining the symptom, root cause hypothesis, and minimal fix plan
- Read the target file, callers, and tests before editing
- Keep the fix narrow unless architecture is the actual cause
- Add or update a regression test
- Run relevant tests and summarize the result
